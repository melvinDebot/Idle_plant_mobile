import { View } from "react-native";
import styled from "styled-components/native";

export const LayoutAnimationContainer = styled(View)`
  position: absolute;
  z-index: 1;

`;

export default {
  LayoutAnimationContainer,
}
